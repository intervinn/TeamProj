<Page x:Class="Client.Views.Pages.StudentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views.Pages" xmlns:ui="http://schemas.microsoft.com/netfx/2009/xaml/presentation"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="StudentsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопки CRUD -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <ui:Button Content="Load" Command="{Binding LoadStudentsCommand}" Margin="5"/>
            <ui:Button Content="Add" Command="{Binding AddStudentCommand}" Margin="5"/>
            <ui:Button Content="Edit" Command="{Binding EditStudentCommand}" Margin="5"/>
            <ui:Button Content="Delete" Command="{Binding DeleteStudentCommand}" Margin="5"/>
        </StackPanel>

        <!-- DataGrid -->
        <ui:DataGrid Grid.Row="1" ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}" 
                     AutoGenerateColumns="False" Margin="5">
            <ui:DataGrid.Columns>
                <ui:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <ui:DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                <ui:DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                <ui:DataGridTextColumn Header="Born At" Binding="{Binding BornAt, StringFormat=dd.MM.yyyy}" Width="100"/>
                <ui:DataGridTextColumn Header="Enlisted At" Binding="{Binding EnlistedAt, StringFormat=dd.MM.yyyy}" Width="100"/>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </Grid>
</Page>
