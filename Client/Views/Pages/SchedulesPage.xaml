<Page x:Class="Client.Views.Pages.SchedulesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Client.Views.Pages" xmlns:ui="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="SchedulesPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопки CRUD -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <ui:Button Content="Load" Command="{Binding LoadSchedulesCommand}" Margin="5"/>
            <ui:Button Content="Add" Command="{Binding AddScheduleCommand}" Margin="5"/>
            <ui:Button Content="Edit" Command="{Binding EditScheduleCommand}" Margin="5"/>
            <ui:Button Content="Delete" Command="{Binding DeleteScheduleCommand}" Margin="5"/>
        </StackPanel>

        <!-- DataGrid -->
        <ui:DataGrid Grid.Row="1" ItemsSource="{Binding Schedules}" SelectedItem="{Binding SelectedSchedule}" 
                     AutoGenerateColumns="False" Margin="5">
            <ui:DataGrid.Columns>
                <ui:DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <ui:DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd.MM.yyyy}" Width="100"/>
                <ui:DataGridTextColumn Header="Grade ID" Binding="{Binding GradeId}" Width="80"/>
                <ui:DataGridTextColumn Header="Lessons" Binding="{Binding Lessons, Converter={x:Static Dialogs:StringToIntCollectionConverter.Default}}" Width="*"/>
            </ui:DataGrid.Columns>
        </ui:DataGrid>
    </Grid>
</Page>
